setLiquidityDeployer.t.sol
├── when caller does not have admin role
│   └── it should revert
└── when caller has admin role
    ├── when new LiquidityDeployer is zero address
    │   └── it should revert
    └── when new LiquidityDeployer is not zero address
        ├── it should set new LiquidityDeployer
        └── it should emit a {SetLiquidityDeployer} event