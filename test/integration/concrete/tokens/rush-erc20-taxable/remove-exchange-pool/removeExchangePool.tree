removeExchangePool.t.sol
├── when caller is not owner
│   └── it should revert
└── when caller is owner
    ├── given exchange pool does not exist
    │   └── it should do nothing
    └── given exchange pool exists
        ├── it should remove address from set of exchange pools
        └── it should emit a {ExchangePoolRemoved} event