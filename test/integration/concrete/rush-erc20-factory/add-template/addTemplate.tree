addTemplate.t.sol
├── when caller does not have admin role
│   └── it should revert
└── when caller has admin role
    ├── when implementation does not support required interface
    │   └── it should revert
    └── when implementation supports required interface
        ├── it should add template
        └── it should emit a {AddTemplate} event